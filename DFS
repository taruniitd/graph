#include <iostream>

void DFS()
{
    for(int i = 0; i < numVertex; i++)
      visited[i] = false;
    DFSUtil();
}
void DFSUtil()
{
  visited[i] = true;
  list<int>::iterator i;
  for(i-> begin; i -> end; i++)
  {
      if(!visited[*i])
        DFS(*i);
  }
}

int main()
{
  DFS();
}
